query ConversationView($appID: ID!, $id: ID!) {
  conversation(appID: $appID, id: $id) {
    messages {
      items {
        id
        text
        created
        user {
          id
          givenName
          familyName
        }
      }
    }
  }
}
