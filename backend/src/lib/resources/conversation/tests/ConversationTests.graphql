mutation AddConversationTest($input: AddConversationInput!) {
  addConversation(input: $input) {
    id
  }
}

query QueryConversationTest($appID: ID!, $id: ID!) {
  conversation(appID: $appID, id: $id) {
    id
  }
}
